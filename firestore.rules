rules_version = '2';

// La app usa Firebase Admin SDK en el backend. El frontend no lee Firestore directo.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
