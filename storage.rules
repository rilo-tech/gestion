rules_version = '2';

// Las fotos de pedidos se suben y leen vía Firebase Admin SDK en el backend.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
