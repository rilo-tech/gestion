# Credenciales (no subir a Git)

Colocá acá el JSON de **Cuentas de servicio** de Firebase Console.

Nombre sugerido:

`rilo-7eff4-firebase-adminsdk.json`

En `.env`:

```
GOOGLE_APPLICATION_CREDENTIALS=credentials/rilo-7eff4-firebase-adminsdk.json
```

Podés generar claves nuevas cuando quieras desde Firebase → Configuración → Cuentas de servicio.
Guardá al menos una copia en un lugar seguro (gestor de contraseñas o backup cifrado).
